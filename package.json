{
  "name": "diveinsteam-web",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "dev:web": "python3 -m http.server 5500",
    "dev:api": "cd api && source .venv/bin/activate && func start --cors http://localhost:5500 --port 7071",
    "dev": "concurrently -n WEB,API \"npm run dev:web\" \"npm run dev:api\"",
    "build": "echo \"no build step\""
  }
}
